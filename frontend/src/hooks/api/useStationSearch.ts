'use client'; import {useQuery} from '@tanstack/react-query'; import {stationService} from '@/services/station.service'; import {queryKeys} from '@/constants/query-keys'; import {useDebounce} from '@/hooks/useDebounce'; export function useStationSearch(value:string){const q=useDebounce(value.trim(),250);return useQuery({queryKey:queryKeys.stations.search(q),queryFn:()=>q?stationService.searchStations(q):stationService.getStations(),staleTime:300000})}
