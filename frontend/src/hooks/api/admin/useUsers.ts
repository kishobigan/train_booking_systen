'use client'; import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query'; import {queryKeys} from '@/constants/query-keys'; import {adminUserService} from '@/services/admin/user.service'; import {useAdminContext} from './useAdminContext'; export function useUsers(params:Record<string,unknown>={}){const {isSuperAdmin}=useAdminContext();return useQuery({queryKey:queryKeys.users.list(params),queryFn:()=>adminUserService.list(isSuperAdmin,params)})} export function useCreateUser(){const {isSuperAdmin}=useAdminContext(),client=useQueryClient();return useMutation({mutationFn:(input:unknown)=>adminUserService.create(isSuperAdmin,input),onSuccess:()=>client.invalidateQueries({queryKey:['users']})})}
