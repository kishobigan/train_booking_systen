'use client'; import {useQuery} from '@tanstack/react-query'; import {journeyService} from '@/services/journey.service'; import {queryKeys} from '@/constants/query-keys'; export function useJourneySearch(params:Record<string,unknown>,enabled=true){return useQuery({queryKey:queryKeys.journeys.search(params),queryFn:({signal})=>journeyService.searchJourneys(params,signal),enabled})}
