'use client'; import {Clock3} from 'lucide-react'; import {useBookingHoldTimer} from '@/hooks/useBookingHoldTimer'; export function BookingHoldTimer({expiresAt,onExpire}:{expiresAt?:string;onExpire?:()=>void}){const timer=useBookingHoldTimer(expiresAt);return <div className={`hold-timer ${timer.expired?'expired':''}`} role="timer"><Clock3 size={18}/>{timer.expired?'Booking hold expired':`Complete payment within ${timer.label}`}{timer.expired&&onExpire&&<button onClick={onExpire}>Refresh status</button>}</div>}
