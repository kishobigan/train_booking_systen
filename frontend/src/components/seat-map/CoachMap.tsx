import Seat from './Seat';
export default function CoachMap({ coach, selectedSeatIds, onSelect }: any) { return <section aria-label={`Coach ${coach.coachNumber}`}><h3>{coach.coachNumber} · {coach.coachClass.replaceAll('_', ' ')}</h3><p>{coach.summary.availableSeats} of {coach.summary.totalSeats} available</p><div style={{ display: 'grid', gridTemplateColumns: `repeat(${coach.layout.columnCount || 4}, minmax(60px, 1fr))`, gap: 8 }}>{coach.seats.map((seat: any) => <Seat key={seat.journeySeatId} seat={seat} selected={selectedSeatIds.includes(seat.journeySeatId)} onSelect={onSelect} />)}</div></section>; }
