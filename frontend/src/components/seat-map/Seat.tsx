type Props = { seat: any; selected: boolean; onSelect: (id: string) => void };
export default function Seat({ seat, selected, onSelect }: Props) { const available = seat.status === 'AVAILABLE'; return <button type="button" className={`seat seat-${selected?'selected':String(seat.status).toLowerCase()}`} aria-label={`${seat.seatNumber}: ${selected ? 'selected' : seat.status.toLowerCase()}`} aria-pressed={selected} disabled={!available} title={`${seat.seatNumber} — ${seat.status}`} onClick={() => onSelect(seat.journeySeatId)}>{seat.isAccessible ? '♿ ' : ''}{seat.isWindow ? '▣ ' : seat.isAisle ? '↔ ' : ''}{seat.seatNumber}</button>; }
