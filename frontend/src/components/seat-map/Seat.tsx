type Props = { seat: any; selected: boolean; onSelect: (id: string) => void };
export default function Seat({ seat, selected, onSelect }: Props) { const available = seat.status === 'AVAILABLE'; return <button type="button" aria-label={`${seat.seatNumber}: ${selected ? 'selected' : seat.status.toLowerCase()}`} aria-pressed={selected} disabled={!available} title={`${seat.seatNumber} — ${seat.status}`} onClick={() => onSelect(seat.journeySeatId)} style={{ padding: 8, border: selected ? '3px solid #174ea6' : '1px solid #777', opacity: available ? 1 : .55, background: selected ? '#cce0ff' : available ? '#e3f6e8' : '#eee' }}>{seat.isAccessible ? '♿ ' : ''}{seat.isWindow ? '▣ ' : seat.isAisle ? '↔ ' : ''}{seat.seatNumber}</button>; }
