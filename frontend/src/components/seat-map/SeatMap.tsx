'use client';
import { useEffect, useState } from 'react'; import { useSeatMapSocket } from '@/hooks/useSeatMapSocket'; import CoachMap from './CoachMap'; import SeatLegend from './SeatLegend'; import SeatMapSummary from './SeatMapSummary'; import SeatMapConnectionStatus from './SeatMapConnectionStatus';
export default function SeatMap(props: { journeyId: string; originJourneyStationId: string; destinationJourneyStationId: string; token?: string; maxSelections?: number; onSelectionChange?: (ids: string[]) => void }) { const { connected, seatMap, error, resync } = useSeatMapSocket(props); const [selected, setSelected] = useState<string[]>([]);
  useEffect(() => { if (!seatMap) return; const available = new Set(seatMap.coaches.flatMap((coach: any) => coach.seats.filter((seat: any) => seat.status === 'AVAILABLE').map((seat: any) => seat.journeySeatId))); setSelected((current) => current.filter((id) => available.has(id))); }, [seatMap]);
  const select = (id: string) => setSelected((current) => { const next = current.includes(id) ? current.filter((value) => value !== id) : current.length < (props.maxSelections || 6) ? [...current, id] : current; props.onSelectionChange?.(next); return next; });
  if (!seatMap) return <div>{error || 'Loading seat map…'}</div>; return <div><SeatMapConnectionStatus connected={connected} />{error && <p role="alert">{error} <button onClick={resync}>Retry</button></p>}<SeatMapSummary summary={seatMap.summary} /><SeatLegend />{seatMap.coaches.map((coach: any) => <CoachMap key={coach.journeyCoachId} coach={coach} selectedSeatIds={selected} onSelect={select} />)}<p>Seat selection is provisional until the booking hold succeeds.</p></div>; }
