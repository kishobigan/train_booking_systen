'use client'; import Link from 'next/link'; import {ReactNode} from 'react'; import {Permission} from '@/constants/permissions'; import {usePermissions} from '@/hooks/usePermissions'; import {useAuthStore} from '@/store/auth.store'; import {LoadingState} from '@/components/ui/StatusState';
export function PermissionPageGuard({permission,anyOf,children}:{permission?:Permission;anyOf?:Permission[];children:ReactNode}){const initialized=useAuthStore(s=>s.initialized),user=useAuthStore(s=>s.user),p=usePermissions();if(!initialized)return <LoadingState label="Checking management access…"/>;const allowed=Boolean(user)&&(permission?p.hasPermission(permission):anyOf?p.hasAnyPermission(...anyOf):true);return allowed?children:<div className="forbidden"><b>403</b><h1>Access denied</h1><p>This page is not available for your account or assigned scope.</p><Link className="button" href="/management/dashboard">Return to dashboard</Link></div>}
