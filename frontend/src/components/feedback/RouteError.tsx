'use client'; import Link from 'next/link'; import {ErrorState} from '@/components/ui/StatusState'; import {ApiError} from '@/services/http/api-error'; export function RouteError({error,reset,back='/journeys'}:{error:Error;reset:()=>void;back?:string}){const requestId=error instanceof ApiError?error.requestId:null;return <div className="shell"><ErrorState message={error.message} retry={reset}/>{requestId&&<p className="center muted">Request ID: {requestId}</p>}<div className="center"><Link href={back}>Return to safety</Link></div></div>}
