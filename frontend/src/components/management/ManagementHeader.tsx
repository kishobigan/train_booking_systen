'use client'; import {LogOut,Menu} from 'lucide-react'; import {useAuthStore} from '@/store/auth.store'; import {authService} from '@/services/modules/auth.service';
export function ManagementHeader(){const {user,clear}=useAuthStore();const logout=async()=>{try{await authService.logout()}finally{clear();location.assign('/login')}};return <header className="management-header"><button className="menu-toggle" aria-label="Toggle management navigation"><Menu/></button><div><b>{user?.fullName}</b><span>{user?.role?.replaceAll('_',' ')}</span></div><button className="button button-secondary" onClick={logout}><LogOut size={17}/> Sign out</button></header>}
