'use client'; import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {TrainFront} from 'lucide-react'; import {managementNavigation} from '@/config/management-navigation'; import {usePermissions} from '@/hooks/usePermissions';
export function ManagementSidebar(){const path=usePathname(),p=usePermissions();const items=managementNavigation.filter(i=>i.permission?p.hasPermission(i.permission):i.anyOf?p.hasAnyPermission(...i.anyOf):true);return <aside className="management-sidebar"><Link className="management-brand" href="/management/dashboard"><TrainFront/> Railway Operations</Link><nav aria-label="Management">{items.map(item=>{const Icon=item.icon;return <Link key={item.href} className={path.startsWith(item.href)?'active':''} href={item.href}><Icon size={19}/>{item.label}</Link>})}</nav></aside>}
