import Link from 'next/link'; import {Card} from '@/components/ui/Card'; export default async function BookingDetails({params}:{params:Promise<{bookingId:string}>}){const {bookingId}=await params;return <div className="shell"><Card><h1>Booking details</h1><p className="muted">Booking {bookingId}</p><Link className="button" href={`/booking/${bookingId}/payment`}>Continue payment</Link></Card></div>}
