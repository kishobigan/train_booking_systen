import { EmptyState } from '@/components/ui/StatusState'; export default async function SeatsPage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="shell"><div className="page-heading"><h1>Choose seats</h1><p className="muted">Journey {id}</p></div><EmptyState title="Select your boarding and destination stations" message="The live seat map activates when a complete segment is selected."/></div>}
