import Link from 'next/link'; import { ArrowRight, ShieldCheck, Radio, TrainFront } from 'lucide-react'; import { Card } from '@/components/ui/Card';
export default function HomePage() { return <div className="shell"><section className="hero"><div><div className="eyebrow">Sri Lanka Railway</div><h1>Your journey, clearly planned.</h1><p className="muted">Search scheduled trains, choose seats in real time, and keep every booking in one secure place.</p><Link className="button" href="/journeys">Find a train <ArrowRight size={18} /></Link></div><Card><div className="stack"><Feature icon={<TrainFront />} title="Simple journey search" text="Compare routes and schedules at a glance."/><Feature icon={<Radio />} title="Live seat availability" text="See holds and confirmations as they happen."/><Feature icon={<ShieldCheck />} title="Reliable payments" text="Protected booking and payment status flows."/></div></Card></section></div> }
function Feature({ icon,title,text }: { icon: React.ReactNode; title:string; text:string }) { return <div style={{display:'grid',gridTemplateColumns:'32px 1fr',gap:12}}><span style={{color:'var(--color-primary)'}}>{icon}</span><div><strong>{title}</strong><div className="muted">{text}</div></div></div> }
