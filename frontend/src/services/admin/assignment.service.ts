import {apiClient} from '@/services/http/api-client'; import {unwrap} from '@/services/http/api-response'; export const assignmentService={assignJourney:async(adminId:string,journeyId:string)=>unwrap((await apiClient.post(`/super-admin/admins/${adminId}/journeys`,{journeyId})).data),removeJourney:async(adminId:string,journeyId:string)=>unwrap((await apiClient.delete(`/super-admin/admins/${adminId}/journeys/${journeyId}`)).data),assignStation:async(staffId:string,stationId:string)=>unwrap((await apiClient.post(`/admin/staff/${staffId}/stations`,{stationId})).data),removeStation:async(staffId:string,stationId:string)=>unwrap((await apiClient.delete(`/admin/staff/${staffId}/stations/${stationId}`)).data)};
